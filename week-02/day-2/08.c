#include <stdio.h>

int main() {
	int h1 = 350;
	int h2 = 200;

	// tell if the double of h2 is bigger than h1

	if (h2*2 > h1) {
        printf("double of h2 is bigger than h1");
	} else {
        printf("double of h2 is smaller than h1");
	}

	return 0;
}